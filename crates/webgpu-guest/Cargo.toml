[package]
name = "webgpu-guest"
version = "0.1.0"
edition.workspace = true
publish = false
readme = "README.md"

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
